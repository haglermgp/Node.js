// NOTE: in order to structure our program into different files, for lucky node provide us a simple module system

// var hello = require('./hello')
// hello.world();
// hello.dog();


var http = require('http') // 
var mysql = require('mysql') //throw err because if not have a installed this packed
