//The globals are objects available to all Node applications without the user having to incorporate any module.
//One of the globals are 'require' >> that include modules into our applications. another of this global we are use is console.log


//The globals help us, know how define key aspects of how Node works

// NOTE: THE global OJECT

// global is the global namespace object (it's similar to widows ins a browser environment, in that it  provides access to global)
